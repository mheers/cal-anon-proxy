---
phase: 04-ci-tests-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/main.yml
  - pages/index.go
autonomous: true
requirements:
  - REQ-09
  - REQ-12

must_haves:
  truths:
    - "Every push to main runs go test -race ./... in CI before the Docker build"
    - "FullCalendar CSS and JS are the same version (6.1.15)"
    - "CI passes without any repository secrets for the test step"
  artifacts:
    - path: ".github/workflows/main.yml"
      provides: "CI pipeline with test step"
      contains: "go test -race ./..."
    - path: "pages/index.go"
      provides: "FullCalendar UI with consistent versions"
      contains: "fullcalendar@6.1.15/main.min.css"
  key_links:
    - from: ".github/workflows/main.yml"
      to: "go test -race ./..."
      via: "Setup Go step + run command"
      pattern: "go test.*-race"
    - from: "pages/index.go"
      to: "https://cdn.jsdelivr.net/npm/fullcalendar@6.1.15/main.min.css"
      via: "h.Link call"
      pattern: "fullcalendar@6\\.1\\.15"
---

<objective>
Add `go test -race ./...` as a CI step before the Docker image build, and fix the FullCalendar CSS/JS version mismatch.

Purpose: Automated testing on every push catches regressions. The FullCalendar CSS version (currently v5.5.1) mismatches the JS (v6.1.15), causing broken calendar UI styling.
Output: Updated `main.yml` with test step; updated `pages/index.go` with matching CDN versions.
</objective>

<execution_context>
@/home/marcel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/marcel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current .github/workflows/main.yml (full content): -->
```yaml
name: dagger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ">=1.23"

      - name: Install Dagger CLI
        run: cd /usr/local && { curl -L https://dl.dagger.io/dagger/install.sh | sh; cd -; }

      - name: Build Image
        working-directory: ci
        run: dagger call build-and-push-image --src ../ --registry-token=env:REGISTRY_ACCESS_TOKEN
        env:
          REGISTRY_ACCESS_TOKEN: ${{ secrets.REGISTRY_ACCESS_TOKEN }}
```

<!-- Current pages/index.go FullCalendar references (lines 9-12): -->
```go
h.Link("https://cdn.jsdelivr.net/npm/fullcalendar@5.5.1/main.min.css", "stylesheet"),       // BUG: v5.5.1 CSS
h.Script("https://cdnjs.cloudflare.com/ajax/libs/ical.js/1.5.0/ical.min.js"),
h.Script("https://cdn.jsdelivr.net/npm/fullcalendar@6.1.15/index.global.min.js"),            // v6.1.15 JS
h.Script("https://cdn.jsdelivr.net/npm/@fullcalendar/icalendar@6.1.15/index.global.min.js"), // v6.1.15 iCal plugin
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add go test step to GitHub Actions</name>
  <files>.github/workflows/main.yml</files>
  <action>
    Insert a new step named "Run tests" between "Setup Go" and "Install Dagger CLI". The step must run:
    ```
    run: go test -race ./...
    ```
    No `env` block or secrets needed — the integration test is guarded by `//go:build integration` and will be excluded automatically.

    The resulting step order must be:
    1. Checkout
    2. Setup Go (existing)
    3. Run tests (NEW) — `go test -race ./...`
    4. Install Dagger CLI (existing)
    5. Build Image (existing)

    Do NOT change anything else in the file.
  </action>
  <verify>
    <automated>grep -n "go test -race" .github/workflows/main.yml && echo "PASS"</automated>
  </verify>
  <done>
    `.github/workflows/main.yml` contains a "Run tests" step with `go test -race ./...` placed before the Dagger build step. No secrets required for the test step.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix FullCalendar CSS version mismatch</name>
  <files>pages/index.go</files>
  <action>
    In `pages/index.go`, change the CSS CDN link from v5.5.1 to v6.1.15:

    OLD: `h.Link("https://cdn.jsdelivr.net/npm/fullcalendar@5.5.1/main.min.css", "stylesheet")`
    NEW: `h.Link("https://cdn.jsdelivr.net/npm/fullcalendar@6.1.15/main.min.css", "stylesheet")`

    The JS lines (v6.1.15) are already correct — do NOT modify them.
    After the change, all three FullCalendar references must be version 6.1.15.

    Verify the build still compiles:
    ```
    go build ./...
    ```
  </action>
  <verify>
    <automated>grep "fullcalendar" pages/index.go | grep -v "6\.1\.15" | wc -l | xargs test 0 -eq && echo "No v5 references remain — PASS"</automated>
  </verify>
  <done>
    `pages/index.go` has no FullCalendar v5.5.1 reference. All three FullCalendar CDN URLs reference v6.1.15. `go build ./...` succeeds.
  </done>
</task>

</tasks>

<verification>
```bash
# Test step is in the workflow
grep -n "go test -race" .github/workflows/main.yml

# No v5 CSS reference remains
grep "5\.5\.1" pages/index.go && echo "FAIL: v5 still present" || echo "PASS: no v5"

# All FullCalendar refs are v6.1.15
grep "fullcalendar" pages/index.go

# Build and unit tests pass
go build ./... && go test -race ./...
```
</verification>

<success_criteria>
- `.github/workflows/main.yml` has `go test -race ./...` step before Dagger build
- `pages/index.go` references `fullcalendar@6.1.15` CSS (matching JS version)
- `go build ./...` exits 0
- `go test -race ./...` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-tests-and-cleanup/04-01-SUMMARY.md`
</output>
